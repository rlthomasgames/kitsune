{
  "events": {
    "restart": "kill-port 3090 && kill-port 3000 && kill-port 8081 && kill-port 8080 && kill-port 27017",
    "crash": "kill-port 3090 && kill-port 3000 && kill-port 8081 && kill-port 8080 && kill-port 27017"
  },
  "delay": "1000"
}