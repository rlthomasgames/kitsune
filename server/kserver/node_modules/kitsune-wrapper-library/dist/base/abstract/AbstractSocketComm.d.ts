import { AbstractModule } from "./AbstractModule";
import { Socket } from "socket.io-client";
import ISockComm from "../interfaces/extensions/ISockComm";
export declare class AbstractSockComm extends AbstractModule implements ISockComm {
    _wrapperConfig: any;
    _assetVendor: any;
    clientMap: Map<string, any | string | boolean | number>;
    socket: Socket;
    id: string;
    totals: Array<number>;
    run(_wrapperConfig: any): ISockComm;
}
